<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Input</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
      <div class="form p-10 w-full">
        <form action="/create" method="post">
          <input
            class="block w-full px-5 py-2 rounded-lg outline-none bg-zinc-800"
            type="text"
            placeholder="Title goes here..."
            name="title"
          />
          <textarea
            class="block w-full mt-2 px-5 py-2 rounded-lg outline-none bg-zinc-800 resize-none"
            placeholder="Write your task details here"
            name="details"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks flex flex-row gap-3 flex-wrap p-10">
        <% if(files.length > 0){ %> <% files.forEach((val)=>{ %>
        <div class="min-w-72 px-3 py-4 bg-zinc-800 p-10 rounded-lg">
          <h1 class="text-white text-3xl tracking-tighter"><%=val%></h1>
          <a class="text-blue-500 inline-block mt-3" href="/file/<%=val%>"
            >Read More.</a
          >
        </div>
        <% }) %> <% } else { %>
        <h3 class="bg-zinc-800">No task yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
